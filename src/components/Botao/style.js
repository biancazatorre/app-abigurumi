import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  button: { backgroundColor: '#CDB7E1', padding: 12, borderRadius: 8, shadowColor: '#000', shadowOpacity: 0.2, shadowRadius: 4, elevation: 5 },
  text: { color: 'black', fontSize: 15, textAlign: 'center', justifyContent: 'center',alignItems: 'center',},
});
